# Core dependencies
typer[all]>=0.9.0
httpx>=0.25.0
pydantic>=2.0.0
pyyaml>=6.0
rich>=13.0.0
pandas>=2.0.0

# Optional dependencies for enhanced features
tabulate>=0.9.0  # For better table formatting
plotly>=5.0.0    # For interactive charts (optional)
jinja2>=3.1.0    # For custom report templates (optional)

# Development dependencies (uncomment if needed)
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
# black>=23.0.0
# flake8>=6.0.0

# Add python-dotenv for loading .env files
python-dotenv 